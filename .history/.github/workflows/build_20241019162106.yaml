name: 打包Nestjs后台接口
on: push

jobs:
  npm-build:
    name: npm - build 工作
    runs-on: ubuntu-latest
    steps:
      - name: 读取仓库内容
        uses: actions/checkout@v4
      - name: 
        run: npm install
      - run: npm run build
